import Link from 'next/link'
import { notFound } from 'next/navigation'
import { requireAppContext } from '@/lib/appContext'
import { detectConflicts,requiresHumanReview,type ConflictRow } from '@/lib/imports/conflicts'
import type { NormalizedImportRow } from '@/lib/imports/contract'
import { applyApprovedImportMatch, decideImportCandidate, publishApprovedImportFinancialFact, confirmPaidFromImport, resolveImportConflict } from './actions'
import { atLeast } from '@/lib/rbac'

export default async function ImportBatchPage({params}:{params:Promise<{id:string}>}){
 const {id}=await params
 const {supabase,membership}=await requireAppContext()
 const {data:batch}=await supabase.from('import_batches').select('id,source_id,original_filename,status,row_count,received_at,parser_key,parser_version,content_sha256,adapter_id,adapter_contract_version,organization_id').eq('id',id).maybeSingle()
 if(!batch)notFound()
 const {data:source}=await supabase.from('import_sources').select('financial_semantic').eq('id',batch.source_id).maybeSingle()
 const {data:adapter}=batch.adapter_id?await supabase.from('integration_adapters').select('adapter_key,provider_key,transport,contract_version').eq('id',batch.adapter_id).maybeSingle():{data:null}
 const {data:rows}=await supabase.from('import_normalized_rows').select('id,raw_row_id,normalization_version,normalized_payload,record_kind,bank_key,external_proposal_number,producer_tax_id,external_table_code,external_table_name,operation_type,term,rate,commission_upfront,commission_deferred').in('raw_row_id',(await supabase.from('import_raw_rows').select('id').eq('batch_id',id)).data?.map(r=>r.id)??[]).limit(200)
 const rowIds=rows?.map(r=>r.id)??[]
 const canSeeCommission=atLeast(membership.role,'supervisor')
 // Same-tenant rows (RLS-scoped) sharing an external proposal number, to surface cross-batch/source conflicts.
 const numbers=[...new Set((rows??[]).map(r=>r.external_proposal_number).filter((x):x is string=>!!x))]
 const {data:peers}=numbers.length?await supabase.from('import_normalized_rows').select('id,raw_row_id,record_kind,bank_key,external_proposal_number,producer_tax_id,external_table_code,external_table_name,operation_type,term,rate,commission_upfront,commission_deferred,amount,normalized_payload').in('external_proposal_number',numbers).limit(500):{data:[]}
 const peerRawIds=[...new Set((peers??[]).map(p=>p.raw_row_id))]
 const {data:peerRaw}=peerRawIds.length?await supabase.from('import_raw_rows').select('id,batch_id').in('id',peerRawIds):{data:[]}
 const peerBatchIds=[...new Set((peerRaw??[]).map(r=>r.batch_id))]
 const {data:peerBatches}=peerBatchIds.length?await supabase.from('import_batches').select('id,source_id,organization_id,received_at').in('id',peerBatchIds):{data:[]}
 const rawToBatch=new Map((peerRaw??[]).map(r=>[r.id,r.batch_id]))
 const batchInfo=new Map((peerBatches??[]).map(b=>[b.id,b]))
 const conflictRows:ConflictRow[]=(peers??[]).flatMap(p=>{
  const b=batchInfo.get(rawToBatch.get(p.raw_row_id)??'')
  if(!b)return []
  const pl=(p.normalized_payload??{}) as Record<string,unknown>
  const normalized:NormalizedImportRow={recordKind:p.record_kind as NormalizedImportRow['recordKind'],bankKey:p.bank_key,externalProposalNumber:p.external_proposal_number,producerTaxId:p.producer_tax_id,externalTableCode:p.external_table_code,externalTableName:p.external_table_name,operationType:p.operation_type,term:p.term,rate:p.rate==null?null:String(p.rate),commissionUpfront:p.commission_upfront==null?null:String(p.commission_upfront),commissionDeferred:p.commission_deferred==null?null:String(p.commission_deferred),amount:p.amount==null?null:String(p.amount),normalizedPayload:pl}
  return [{ref:p.id,organizationId:b.organization_id,sourceId:b.source_id,providerKey:adapter?.provider_key??null,batchId:b.id,occurredAt:typeof pl.occurredAt==='string'?pl.occurredAt:null,normalized}]
 })
 const conflicts=detectConflicts(conflictRows).filter(f=>f.rowRefs.some(ref=>rowIds.includes(ref)))
 // Persistent register (import_conflicts_v1). supervisor+ only (RLS). Three explicit states: store missing (migration not
 // applied), store failing (other error: never hidden), store available (possibly empty).
 const {data:persistedConflictsRaw,error:persistedConflictsError}=canSeeCommission?await supabase.from('import_conflicts').select('id,kind,severity,identity_key,detail,status,resolution_note,resolved_at,created_at').eq('batch_id',id).order('created_at',{ascending:false}).limit(200):{data:[],error:null}
 const storeMissing=!!persistedConflictsError&&['42P01','PGRST205'].includes(String((persistedConflictsError as {code?:string}).code))
 const storeFailed=!!persistedConflictsError&&!storeMissing
 const persistedConflicts=[...(persistedConflictsRaw??[])].sort((a,b)=>(a.status==='open'?0:1)-(b.status==='open'?0:1))
 const persistedIds=persistedConflicts.map(c=>c.id)
 const {data:conflictRowLinks}=persistedIds.length?await supabase.from('import_conflict_rows').select('conflict_id,raw_row_id').in('conflict_id',persistedIds):{data:[]}
 const {data:linkedRaw}=(conflictRowLinks??[]).length?await supabase.from('import_raw_rows').select('id,row_number').in('id',[...new Set((conflictRowLinks??[]).map(l=>l.raw_row_id))]):{data:[]}
 const rowNo=new Map((linkedRaw??[]).map(r=>[r.id,r.row_number]))
 const rowsOfConflict=(cid:string)=>(conflictRowLinks??[]).filter(l=>l.conflict_id===cid).map(l=>rowNo.get(l.raw_row_id)).filter(x=>x!=null).sort((a:number,b:number)=>a-b)
 const {data:candidates}=rowIds.length?await supabase.from('import_match_candidates').select('id,normalized_row_id,match_strength,status,proposal_id,product_table_id,channel_id').in('normalized_row_id',rowIds):{data:[]}
 const candidateIds=candidates?.map(c=>c.id)??[]
 const {data:decisions}=candidateIds.length?await supabase.from('import_decisions').select('id,candidate_id,decision,decided_at').in('candidate_id',candidateIds).order('decided_at',{ascending:false}):{data:[]}
 const decisionIds=decisions?.map(d=>d.id)??[]
 const {data:applied}=decisionIds.length?await supabase.from('import_applied_decisions').select('decision_id,applied_at').in('decision_id',decisionIds):{data:[]}
 const byRow=new Map((candidates??[]).map(c=>[c.normalized_row_id,c]))
 type ReviewDecision={id:string;candidate_id:string;decision:string;decided_at:string}
 const latestDecision=new Map<string,ReviewDecision>()
 for(const d of decisions??[])if(!latestDecision.has(d.candidate_id))latestDecision.set(d.candidate_id,d as ReviewDecision)
 const appliedSet=new Set((applied??[]).map(a=>a.decision_id))
 const canReview=atLeast(membership.role,'supervisor')
 const financialSemantic=source?.financial_semantic??'unclassified'
 const {data:financialLinks}=decisionIds.length?await supabase.from('financial_evidence_links').select('import_decision_id').in('import_decision_id',decisionIds):{data:[]}
 const financiallyPublished=new Set((financialLinks??[]).map(x=>x.import_decision_id))
 return <section>
  <Link href="/app/importacoes" className="text-sm text-slate-400">← Importações</Link>
  <h1 className="mt-3 text-3xl font-semibold">{batch.original_filename}</h1>
  <p className="mt-2 text-sm text-slate-400">Status {batch.status} · parser {batch.parser_key??'—'} {batch.parser_version??''} · SHA-256 {batch.content_sha256.slice(0,12)}…</p><div className="mt-3 rounded-lg border border-slate-800 p-3 text-xs text-slate-300"><strong>Linhagem</strong> · adapter {adapter?`${adapter.adapter_key} (${adapter.provider_key}/${adapter.transport}) contrato ${batch.adapter_contract_version??adapter.contract_version}`:'não vinculado ao catálogo'} · normalização {rows?.[0]?.normalization_version??'—'}{(()=>{const sc=(rows?.[0]?.normalized_payload as {schema?:{state?:string;fingerprint?:string;fingerprint_known?:boolean;unmapped_headers?:string[]}}|undefined)?.schema;return sc?<span> · schema {sc.state} · impressão {sc.fingerprint?.slice(0,12)}… {sc.fingerprint_known?'(validada)':'(não validada contra arquivo real)'}{sc.unmapped_headers?.length?` · ${sc.unmapped_headers.length} colunas fora do contrato (preservadas no raw)`:''}</span>:null})()}</div>
  {storeFailed&&<div role="alert" className="mt-3 rounded-lg border border-red-500/40 bg-red-500/10 p-3 text-xs text-red-200">Não foi possível consultar o registro persistente de conflitos. Os achados abaixo são apenas calculados.</div>}
  {persistedConflicts.length>0&&<div className="mt-3 rounded-lg border border-amber-500/40 bg-amber-500/5 p-3 text-xs text-amber-200"><strong>{persistedConflicts.length} conflito(s) persistido(s)</strong> ({persistedConflicts.filter(c=>c.status==='open').length} abertos) — registro de evidência ligado às linhas raw; nunca autoriza publicação financeira automática.<div className="mt-2 space-y-2">{persistedConflicts.slice(0,50).map(f=><div key={f.id} className="rounded border border-amber-500/20 p-2"><div>{f.kind}{f.identity_key?` · ${f.identity_key}`:''} · {f.severity} · {f.status}{rowsOfConflict(f.id).length?` · linhas ${rowsOfConflict(f.id).join(', ')}`:''}</div>{f.status==='open'&&canReview&&<form action={resolveImportConflict} className="mt-2 flex flex-wrap gap-2"><input type="hidden" name="batch_id" value={id}/><input type="hidden" name="conflict_id" value={f.id}/><input name="resolution_note" required minLength={10} maxLength={2000} placeholder="Justificativa (10+ caracteres)" className="min-w-64 rounded border border-slate-700 bg-slate-950 px-2 py-1"/><button name="status" value="resolved" className="rounded border border-slate-600 px-2 py-1">Resolver</button><button name="status" value="dismissed" className="rounded border border-slate-600 px-2 py-1">Descartar com justificativa</button></form>}{f.resolution_note&&<div className="mt-1 text-slate-400">{f.resolution_note}{f.resolved_at?` · ${new Date(f.resolved_at).toLocaleString('pt-BR')}`:''}</div>}</div>)}</div></div>}
  {conflicts.length>0&&<div className="mt-3 rounded-lg border border-amber-500/40 bg-amber-500/5 p-3 text-xs text-amber-200"><strong>{conflicts.length} achado(s) calculado(s) nesta visualização (inclui outros lotes/fontes do tenant)</strong> — {storeMissing?'o registro persistente ainda não existe no banco':canReview?'não substituem o registro persistente':'visíveis para acompanhamento'}; nenhum publica fato financeiro.<ul className="mt-2 list-disc pl-5">{conflicts.slice(0,20).map((f,i)=><li key={i}>{f.kind}{f.identityKey?` · ${f.identityKey}`:''} ({f.severity})</li>)}</ul></div>}
  <div className="mt-3 inline-flex rounded-lg border border-slate-700 px-3 py-2 text-xs text-slate-300">Semântica financeira: {financialSemantic}{financialSemantic==='commercial_offer'?' · não publica recebimento':''}</div>
  <div className="mt-6 overflow-x-auto rounded-xl border border-slate-800">
   <table className="min-w-full text-left text-sm"><thead className="bg-slate-900 text-slate-400"><tr><th className="p-3">Banco</th><th className="p-3">Proposta/Tabela</th><th className="p-3">Operação</th><th className="p-3">Prazo</th>{canSeeCommission&&<th className="p-3">Comissão</th>}<th className="p-3">Evidência de origem</th><th className="p-3">Matching</th></tr></thead>
   <tbody>{!rows?.length?<tr><td colSpan={7} className="p-5 text-slate-400">Nenhuma linha normalizada.</td></tr>:rows.map(r=>{const m=byRow.get(r.id);return <tr key={r.id} className="border-t border-slate-800"><td className="p-3">{r.bank_key??'—'}</td><td className="p-3">{r.external_proposal_number??r.external_table_code??'—'}<div className="text-xs text-slate-500">{r.external_table_name}</div></td><td className="p-3">{r.operation_type??r.record_kind}</td><td className="p-3">{r.term??'—'}</td>{canSeeCommission&&<td className="p-3">{r.commission_upfront??'—'}{r.commission_deferred&&<span className="text-slate-500"> + diferido {r.commission_deferred}</span>}</td>}<td className="p-3 text-xs text-slate-400">{(()=>{const pl=(r.normalized_payload??{}) as {source_status?:Record<string,string|null>;source_commission?:{repasse_value:string|null;state:string};quarantine?:{reason:string}|null};const st=pl.source_status;return <>{st&&Object.entries(st).filter(([,v])=>v).map(([k,v])=><div key={k}>{k}: {v}</div>)}{canSeeCommission&&pl.source_commission&&<div>repasse: {pl.source_commission.repasse_value??'não informado'} ({pl.source_commission.state}; ausência não implica receita zero)</div>}{pl.quarantine&&<div className="text-amber-300">quarentena: {pl.quarantine.reason}</div>}{conflicts.some(f=>f.rowRefs.includes(r.id)&&requiresHumanReview([f],r.id))&&<div className="text-amber-300">conflito: revisão humana</div>}{!st&&!pl.quarantine&&'—'}</>})()}</td><td className="p-3">{m?<><strong>{m.match_strength}</strong><div className="text-xs text-slate-500">{m.status}</div>{canReview&&!latestDecision.get(m.id)&&m.status==='suggested'&&<form action={decideImportCandidate} className="mt-2 flex flex-wrap gap-1"><input type="hidden" name="batch_id" value={id}/><input type="hidden" name="normalized_row_id" value={r.id}/><input type="hidden" name="candidate_id" value={m.id}/>{m.match_strength!=='ambiguous'&&<button name="decision" value="approve" className="rounded border border-slate-700 px-2 py-1 text-xs">Aprovar</button>}<button name="decision" value="reject" className="rounded border border-slate-700 px-2 py-1 text-xs">Rejeitar</button><button name="decision" value="human_required" className="rounded border border-slate-700 px-2 py-1 text-xs">Revisar</button></form>}{latestDecision.get(m.id)?.decision==='approve'&&!appliedSet.has(latestDecision.get(m.id)!.id)&&canReview&&<form action={applyApprovedImportMatch} className="mt-2"><input type="hidden" name="batch_id" value={id}/><input type="hidden" name="decision_id" value={latestDecision.get(m.id)!.id}/><button className="rounded bg-emerald-500 px-2 py-1 text-xs font-medium text-slate-950">Aplicar vínculo aprovado</button></form>}{latestDecision.get(m.id)?.decision==='approve'&&appliedSet.has(latestDecision.get(m.id)!.id)&&financialSemantic==='production_report'&&r.record_kind==='status'&&canReview&&<form action={confirmPaidFromImport} className="mt-2"><input type="hidden" name="batch_id" value={id}/><input type="hidden" name="decision_id" value={latestDecision.get(m.id)!.id}/><button className="rounded bg-sky-400 px-2 py-1 text-xs font-medium text-slate-950">Confirmar PAID por evidência</button></form>}{latestDecision.get(m.id)?.decision==='approve'&&appliedSet.has(latestDecision.get(m.id)!.id)&&!['commercial_offer','production_report'].includes(financialSemantic)&&!financiallyPublished.has(latestDecision.get(m.id)!.id)&&canReview&&<form action={publishApprovedImportFinancialFact} className="mt-2"><input type="hidden" name="batch_id" value={id}/><input type="hidden" name="decision_id" value={latestDecision.get(m.id)!.id}/><button className="rounded bg-amber-400 px-2 py-1 text-xs font-medium text-slate-950">Publicar fato financeiro comprovado</button></form>}{latestDecision.get(m.id)&&<div className="mt-1 text-xs text-slate-500">decisão: {latestDecision.get(m.id)!.decision}{appliedSet.has(latestDecision.get(m.id)!.id)?' · aplicada':''}{financiallyPublished.has(latestDecision.get(m.id)!.id)?' · financeiro publicado':''}</div>}</>:<span className="text-slate-500">não avaliado</span>}</td></tr>})}</tbody></table>
  </div>
  <p className="mt-4 text-xs text-slate-500">Matching ambíguo permanece para revisão humana. Esta tela não publica tabela, proposta ou valor financeiro.</p>
 </section>
}
